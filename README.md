# 🖥️ Exam Network Manager

Система удалённого управления компьютерами студентов во время демонстрационного экзамена.  
Позволяет администратору централизованно:
- Подключаться к клиентским ПК (агентам);
- Загружать им файлы или целые папки;
- Управлять файловой системой;
- Очищать каталоги;
- Получать список файлов и путь к рабочему столу.

---

## 📦 Архитектура

Проект состоит из двух частей:

| Компонент | Назначение | Технология |
|------------|-------------|-------------|
| **Admin Server (FastAPI)** | Центральный сервер, принимает подключения агентов и команды от администратора | Python 3.10+ |
| **Student Agent** | Фоновое приложение на клиентском ПК, взаимодействует с сервером по WebSocket | .NET 9.0 / C# |

---

## ⚙️ Возможности

- 📡 **Автоматическое подключение** клиента к серверу по имени пользователя (`Environment.UserName`);
- 📂 **Загрузка файлов** на указанный путь;
- 📦 **Передача и распаковка папок** (через ZIP);
- 🧹 **Очистка каталогов**, кроме `.exe` и `.lnk`;
- 🗂️ **Получение структуры файлов и папок** в JSON;
- 💾 **Получение пути до рабочего стола пользователя**.

---

## 🚀 Установка и запуск

### 🧠 Требования
- **Python 3.10+**
- **.NET SDK 9.0+**

---

### 🔧 1. Сервер (Python / FastAPI)

Установите зависимости:
```bash
pip install fastapi uvicorn
```

Запустите сервер:
```bash
uvicorn main:app --port 8000
```

После запуска консоль покажет:
```makefile
INFO:     Application startup complete.
```

Теперь сервер слушает по адресу
👉 `ws://<ip_сервера>:8000/ws/{client_id}`

### 🧩 2. Клиент (C# агент)

Соберите проект:
```bash
dotnet build -c Release
```

Запусти агент вручную или через автозапуск::
```bash
./Student.exe --id=student01 --server=10.137.203.94
```
или
```bash
./Student.exe -i student01 -s 10.137.203.94
```

- агрумент `student01` - ID студента для определения
- агрумент `10.137.203.94` - IP сервера

> Если аргумент не передан, клиент автоматически использует имя текущего пользователя (Environment.UserName) как идентификатор.

### 🧰 REST API (для администратора)

#### 📜 Список подключённых клиентов
```bash
GET /clients
```

Пример ответа:
```json
{
  "connected": ["Ivanov-AS", "Petrov-01"],
  "count": 2
}
```

#### 📁 Отправка файла клиенту
```bash
POST /upload/{client_id}
```
Параметры (form-data):
- `file`: файл для загрузки
- `target_path`: путь назначения (по умолчанию `C:\ExamFiles`)

Пример:
```bash
curl -X POST -F "file=@Doc1.docx" -F "target_path=C:\Users\{client_id}\Desktop" http://127.0.0.1:8000/upload/Ivanov-AS
```

#### 📦 Отправка целой папки
```bash
POST /upload/{client_id}
```
Параметры (form-data):
- `source_path`: путь к папке на сервере
- `target_path`: путь назначения на клиенте

Пример:
```bash
curl -X POST "http://127.0.0.1:8000/upload/{client_id}" -F "source_path=C:/Users/Ivanov-AS.NATK/Downloads/PR3CreatingAWPF-master" -F "target_path=C:/Users/Ivanov-AS.NATK/Desktop"
```

#### 🗑️ Очистка папки на клиенте
```bash
POST /clean_folder/{client_id}
```
Параметры (form-data):
- `path`: путь к директории

Пример:
```bash
curl -X POST -F "path=C:\Users\{client_id}\Desktop\teststr" http://127.0.0.1:8000/clean_folder/Ivanov-AS
```

>Удаляет всё содержимое кроме `.exe` и `.lnk` файлов.

#### 📂 Просмотр содержимого папки
```bash
POST /send/{client_id}?command=listdir:<path>
```
Пример:
```bash
curl -X POST "http://127.0.0.1:8000/send/{client_id}?command=listdir:C:\Users\Ivanov-AS\Desktop"
```

Ответ:
```json
{
  "status": "ok",
  "result": {
    "folders": ["C:\\Users\\Ivanov-AS\\Desktop\\Folder1"],
    "files": ["C:\\Users\\Ivanov-AS\\Desktop\\Doc1.docx"]
  }
}
```

#### 💻 Получение пути до рабочего стола
```bash
POST /send/{client_id}?command=get_desktop_path
```

Ответ:
```json
{
  "status": "ok",
  "result": "C:\\Users\\Ivanov-AS\\Desktop"
}
```
